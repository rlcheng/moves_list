<%= form_for([@character, @character.moves.build]) do |m| %>
	<p>
		<%= m.label :name %><br>
		<%= m.text_field :name %>
	</p>
	<p>
		<%= m.label :input %><br>
		<%= m.text_field :input %>
	</p>	
	<p>
		<%= m.submit %>
	</p>
<% end %>
