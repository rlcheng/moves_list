<p>
	<strong>Title:</strong>
	<%= @game.title %>
</p>

<h2>Characters</h2>
<%= render @characters %>
<% if current_user %>
<h2>Add a character:</h2>
<%= render 'characters/form' %>
<% end %>

<%= link_to 'Back', games_path %>
