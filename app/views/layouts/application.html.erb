<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>moves_list</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<div class='navbar navbar-default navbar-fixed-top'>
  <div class='container'>
    <a class='fa fa-gamepad navbar-brand' href='/'> Move List</a>

    <button type='button' class='navbar-toggle'
            data-toggle='collapse'
            data-target='.navbar-collapse'>
      <span class='sr-only'>Login</span>
      <span class='icon-bar'></span>
      <span class='icon-bar'></span>
      <span class='icon-bar'></span>
    </button>

    <ul class='nav navbar-nav navbar-right collapse navbar-collapse'>
      <li>
        <%= form_tag search_path, method: :get, class: 'navbar-form', role: 'search' do %>
          <div class="input-group">
              <%= text_field_tag :q, params[:q], class: 'form-control', placeholder: 'Search...' %>
              <div class="input-group-btn">
                  <button class="btn btn-default" type="submit"><i class="fa fa-search"></i></button>
              </div>
          </div>
        <% end %>
      </li>
      <li><%= link_to " About", games_about_path, class: "fa fa-info-circle" %></li>
      <li><%= link_to " Help", games_help_path, class: "fa fa-question-circle" %></li>
      <% if current_user %>
        <li><%= link_to " Log Out", log_out_path, class: "fa fa-sign-out" %></li>
      <% else %>
        <li><%= link_to " Sign Up", sign_up_path, class: "fa fa-user-plus" %></li>
        <li><%= link_to " Log In", log_in_path, class: "fa fa-sign-in" %></li>
      <% end %>
    </ul>
  </div>
</div>

<body>
  <% flash.each do |key, value| %>
    <% if key == "error" %>
    <div class='alert alert-danger' role='alert'>    
    <% else %>
    <div class='alert alert-success' role='alert'>
    <% end %>
      <div class="<%= key %>"><%= value %></div>
    </div>
  <% end %>

  <div class='container well'>
    <%= yield %>
  </div>
</body>

</html>
